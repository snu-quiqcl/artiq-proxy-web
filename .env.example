# Copy to `.env` (gitignored) and adjust. Loaded from repo root and/or `artiq_proxy_web/`.

# ARTIQ master (required; no defaults). RPC host is ::1 in settings.
ARTIQ_MASTER_PORT=3251
ARTIQ_MASTER_PATH=/path/to/artiq
